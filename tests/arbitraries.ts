// Cross-cutting fast-check arbitraries land here in Phase 1, alongside the
// first real math blocks. See docs/TESTING.md for the planned generator
// surface (realScalar, realMatrix, integerMatrix, invertibleMatrix,
// positiveDefiniteMatrix, distribution, expression, ...).
//
// Phase 0: stub. Empty-export keeps the module loadable.

export {};
